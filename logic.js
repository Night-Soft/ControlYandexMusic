checker = false;
//pauseTarget = null;
if (checker == false) {
    let pauseTarget = document.getElementsByClassName("player-controls__btn_pause");
    pauseTarget[0].click();
    checker = true;
} else {
    let pauseTarget = document.getElementsByClassName("player-controls__btn_pause");
    pauseTarget[0].click();
}

// function create() {
//     let pauseTarget = document.getElementsByClassName("player-controls__btn_pause");
// }

//delete pauseTarget;